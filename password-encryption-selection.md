# パスワード暗号化ライブラリ 技術選定（JSON Server用）

## 目的
現在、ログイン時に使用しているパスワードが平文（そのままの文字）で保存されています。  
このままでは、ファイルを見れば誰でもパスワードを確認できてしまうため、セキュリティ上の大きなリスクがあります。  

そのため、安全にパスワードを保存するための暗号化ライブラリを選定しました。

---

## 候補ライブラリ

| ライブラリ名 | 種類 | 概要 | 長所 | 短所 | 導入方法 | 補足 |
|---------------|--------|------|------|------|----------|------|
| **bcryptjs** | ハッシュ化（戻せない） | パスワードを「元に戻せない形」に変換して保存する | ・実績が多く安全性が高い<br>・同じパスワードでも毎回違う結果になる（ソルト）<br>・今後サーバーを本格化してもそのまま使える | ・少し処理が重い（問題ない程度） | `npm install bcryptjs` | Node.js / JSON Server で利用可能 |
| **crypto-js** | 暗号化（戻せる） | 「鍵」を使って暗号化・復号できる | ・設定が簡単<br>・テストがしやすい | ・鍵が漏れるとパスワードを復元できてしまう | `npm install crypto-js` | Node.js / ブラウザ両対応 |
| **argon2-browser** | ハッシュ化（戻せない） | bcryptより新しい技術 | ・より強力な暗号化が可能 | ・ブラウザ実行前提で安定性が不十分<br>・導入が複雑 | `npm install argon2-browser` | ブラウザ専用、Node.js では利用不可 |
| **SHA-256 (Node.js内蔵)** | ハッシュ化（戻せない） | シンプルな方式 | ・外部ライブラリ不要 | ・計算が高速すぎるためブルートフォース攻撃に弱い（対策としてPBKDF2やソルト・pepper、反復回数の導入が必要）<br>・bcryptより安全性が低い | なし | Node.js 標準モジュールで利用可能。単体での使用は推奨されない |

---

## 用語の説明

- **ハッシュ化**：一方向の変換。  
  変換したら元のパスワードには戻せないため、安全性が高い。

- **暗号化**：鍵を使って変換。  
  鍵を使えば元に戻せる。便利だが鍵が漏れると危険。

---

## SHA‑256 の注意点

SHA‑256 は計算が非常に速いため、攻撃者が大量の候補パスワードを短時間で試せてしまい、ブルートフォース攻撃や辞書攻撃に脆弱になりやすいです。単に SHA‑256 を使うだけでは不十分で、以下の対策が必要です。

- **鍵伸張（Key stretching）を使用**：PBKDF2 / scrypt / Argon2 のように反復回数やメモリコストで「計算を重くする」  
- **ソルト（salt）の付与**：同じパスワードでも異なるハッシュになるようにランダム値を付加  
- **ペッパー（pepper）の活用**：サーバ側で管理される秘密値を混ぜる  
- **遅いハッシュ関数を優先**：bcrypt や Argon2 の使用が望ましい  
- **定期的な見直し**：ハッシュアルゴリズムやパラメータを更新し、古いパスワードはログイン時に再ハッシュ  

---

## 結論：第一候補 bcryptjs

### 選定理由
1. パスワードを戻せない形式で安全に保存できる  
2. 実績が多く、セキュリティの信頼性が高い  
3. JSON Serverのような簡易環境でも動作する  
4. 将来的に本格的なバックエンド（Spring BootやExpressなど）に移行しても互換性がある  
5. 導入が簡単で npm 1行で利用可能  

---

## 実装イメージ（bcryptjs）

```js
import bcrypt from "bcryptjs";

// 登録時（保存前にハッシュ化）
const hashedPassword = bcrypt.hashSync(password, 10);

// ログイン時（照合）
const isMatch = bcrypt.compareSync(inputPassword, user.password);

if (isMatch) {
  console.log("ログイン成功");
} else {
  console.log("パスワード不一致");
}
